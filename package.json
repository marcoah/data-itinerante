{
  "name": "data-itinerante-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node -e \"require('fs').mkdirSync('.wrangler/registry', { recursive: true }); require('fs').mkdirSync('.wrangler/state', { recursive: true });\" && wrangler dev --persist-to .wrangler/state",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "^3.111.0"
  }
}
